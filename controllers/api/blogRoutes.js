// blog routes go here
//  similar to module 13
const router = require('express').Router();
const { Comment, User, Blog } = require('../../models');
// do commentRoutes.js first then do this one.
// commentRoutes.js copy and paste it here and edit accordingly

module.exports = router;