<div class="card">
    <div class="card-header">
        <h5 class="card-title" id="title" name={{id}}>{{title}}</h5>
    </div>
    <div class="card-body">

        <p class="card-text">{{body}}</p>

    </div>
    <div class="card-footer text-muted">
        {{format_time creation_date}}
    </div>
</div>
{{!-- render comment section --}}
{{> comment-section}}